#include "Config.hpp"
/*
namespace alog
{

	Config::Config
	(
		std::string message, 
		std::weak_ptr<std::ostream> lastStream, 
		bool nameShown, 
		logLevel level, 
		entityClass entClass
	):
		lastMessage(message), 
		outputStream(lastStream), 
		nameShown(nameShown),
		lastLevel(level),
		lastClass(entClass)

	{
		return;
	}

	Config::~Config()
	{
		return;
	}

}*/
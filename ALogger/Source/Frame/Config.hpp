#pragma once
/*
#include <string>
#include <memory>

#include "../Authority/Authority.hpp"
#include "../Frame/Level.hpp"

namespace alog
{

	class Config
	{
	public:
		std::string lastMessage;
		std::shared_ptr<std::ostream> outputStream;
		bool nameShown;
		logLevel lastLevel;
		entityClass lastClass;
		//Frame*

	public: 
		Config
		(
			std::string message, 
			std::weak_ptr<std::ostream> lastStream,
			bool nameShown,
			logLevel level,
			entityClass entClass
		);
		~Config();
	};

}*/